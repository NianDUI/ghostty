node_modules/
dist/
web-bundle/
# Local deploy target — copy deploy.env.example to deploy.env and
# fill in the real DEPLOY_HOST / DEPLOY_PATH for your relay.
deploy.env
# Playwright artefacts (browser binaries live in ~/Library, no need to ignore)
playwright-report/
test-results/
.playwright/
