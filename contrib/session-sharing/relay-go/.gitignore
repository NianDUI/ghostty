# Local deploy target — copy deploy.env.example to deploy.env and fill
# in the real DEPLOY_HOST / DEPLOY_PATH for your relay.
deploy.env
# Compiled relay binary (produced by `go build -o ghostty-relay .` or
# deploy.sh). The deploy script cross-compiles a fresh one for the target;
# never commit a host-arch binary.
/ghostty-relay
